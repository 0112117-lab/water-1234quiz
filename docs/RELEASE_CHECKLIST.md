# RELEASE_CHECKLIST.md — 정수퀴즈 배포 점검표

> 배포 = `index.html`을 GitHub 저장소 `0112117-lab/water-1234quiz` 루트에 덮어쓰기 커밋.
> 배포 전 아래를 순서대로 확인한다. **로컬 파일이 아니라 https 라이브 URL에서 최종 확인할 것.**

---

## A. 배포 전 — 데이터 무결성

- [ ] 총 문항 수 확인 (현재 기준값 **2,038**). 의도한 증감과 일치하는가?
- [ ] 신규/수정 회차의 문항 번호가 **1~80 완전**(누락·중복 없음).
- [ ] 모든 문항 `a`(정답) 값이 **1~4** 범위.
- [ ] 모든 문항 필드(`g, gt, s, n, q, c, e`) 결손 0, `c` 배열 길이 4.
- [ ] 불확실 정답 항목에 `(교재 정답표 기준)` 표기 유지.
- [ ] 신규 문항이 `/*__DATA__*/` 마커 **앞**에 삽입됨.

**검증 스니펫(참고):**
```js
// QUESTIONS 배열 파싱 후
arr.filter(q=>!q.g||!q.gt||!q.s||!q.q||!Array.isArray(q.c)||q.c.length!==4||!(q.a>=1&&q.a<=4)||!q.e).length === 0
```

## B. 배포 전 — 코드 정상성

- [ ] `<script>` 구문 오류 없음(브라우저 콘솔 에러 0).
- [ ] `QUESTIONS = [ ... ];` 배열이 정상 종료(`];`), 마커 손상 없음.
- [ ] 최근 수정한 함수 열림/닫힘 중괄호 균형 확인.
- [ ] `<head>` 매니페스트/아이콘 data URI 문자열 손상 없음(매니페스트 JSON 파싱 OK).

## C. 배포 전 — 기능 스모크 테스트 (실기기 권장)

- [ ] 홈 로드, 진행률·연속정답 표시 정상.
- [ ] **이어풀기:** 한 장 완료 후 → **다음 미완료 장으로 이동**하는지.
- [ ] 문제풀이: 보기 선택 → 정오/해설 표시, 이전/다음/번호점프.
- [ ] 과목별·셔플·처음부터 모드 동작.
- [ ] 오답노트·북마크 목록 반영.
- [ ] 백업(클립보드 복사) → 복원 왕복 정상.
- [ ] **AI 질문하기:** 프롬프트 클립보드 복사됨 + 클로드/GPT 이동(Android 앱, iOS 웹).
- [ ] 초기화 확인창 동작.

## D. 배포 — GitHub

- [ ] 파일명 **`index.html`** 그대로 저장소 루트에 업로드/덮어쓰기.
- [ ] 커밋 메시지 작성(예: `문항 추가(제30·29·28·27회 2급, 제34·33·32회 3급) + 이어풀기 개선 + PWA`).
- [ ] Commit changes 클릭.

## E. 배포 후 — 라이브 확인 (https)

- [ ] `https://0112117-lab.github.io/water-1234quiz/` 접속(캐시 강제 새로고침).
- [ ] 변경 내용 반영 확인(문항 수/기능).
- [ ] **삼성인터넷:** 설정 → "📲 홈 화면에 추가하기" 버튼 동작 또는 메뉴 안내 노출.
- [ ] iOS Safari: 공유 → 홈 화면에 추가로 아이콘 생성.
- [ ] 기존 사용자 학습기록(localStorage `jsq_v1`) 유지되는지(스토리지 키 변경 금지).

## F. 롤백 계획

- [ ] 문제 발생 시 GitHub 커밋 히스토리에서 직전 커밋으로 되돌리기(Revert).
- [ ] 로컬에 직전 정상 `index.html` 백업본 1개 보관.

---

### 주의 (되돌리기 어려움 표시)

- **localStorage 키(`jsq_v1`) 변경** → 기존 사용자 학습기록 전부 유실. 되돌릴 수 없음. 변경 금지(마이그레이션 코드 없이는).
- **문항 `g`(group key) 변경** → 해당 회차의 기존 응답 기록이 매칭 안 되어 사실상 초기화됨. 신중히.
